import 'package:flutter/material.dart';

class BodyCreate extends StatelessWidget {
  const BodyCreate({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("body create"),
    );
  }
}
